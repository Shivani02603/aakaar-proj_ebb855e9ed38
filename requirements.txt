# Floor pins (>=), not exact pins: exact pins from one era stop having binary wheels when
# the user's Python moves on (pydantic==2.5 has no Python-3.14 wheels → the whole install
# aborts). Floors keep known-good minimums while letting pip resolve an installable version.
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0.29
psycopg2-binary>=2.9.9
pydantic[email]>=2.6
bcrypt>=4.1
python-jose[cryptography]>=3.3
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
pytest>=8.1
